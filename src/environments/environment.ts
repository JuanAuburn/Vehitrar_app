export const environment = {
  production: false,
  firebaseConfig: {
    // get the firebaseConfig in firebase / console / project overview / config project
  },
  GOOGLE_CLIENT_ID: '', // get this in https://console.developers.google.com/apis/dashboard / credentials / ID of clients OAuth 2.0
  timeOutToService: 3000,
  pageNumberElements: 10,
  pageSearchsToTimestampDiference: 5356800000
};
